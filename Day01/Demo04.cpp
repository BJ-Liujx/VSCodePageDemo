#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    double a;
    a = sqrt(1.234);
    a = pow(2,3);
    a = rand();
    bool b = true;
    cout << a << endl;
    if (b)
    {
        cout << b;
    }

    return 0;
}
